```mermaid
erDiagram
    movie {
        varchar movie_ID PK
        varchar title
        varchar type
        int runtime
        date release_date
        varchar director
        varchar starring
    }

    customer {
        varchar c_ID PK
        varchar name
        varchar phone
    }

    hall {
        varchar hall_ID PK
        varchar mode
        int capacity
        varchar location
    }

    schedule {
        varchar schedule_ID PK
        date date
        time time
        decimal price
        int number
    }

    ticket {
        varchar ticket_ID PK
        varchar seat_num
    }

```